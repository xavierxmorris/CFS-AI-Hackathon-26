# OrderProcessor — Flow Diagram

> Generated by Copilot CLI from: `@legacy-code/OrderProcessor.cs`

---

## Happy-Path Sequence Diagram

```mermaid
sequenceDiagram
    participant Client
    participant OP as OrderProcessor
    participant CR as CustomerRepository
    participant DB as SQL Server
    participant PS as PaymentService
    participant GW as QuickPay Gateway
    participant SMTP as Email Server

    Client->>OP: ProcessOrder(customerId, productCode, qty, token)

    OP->>CR: GetCustomerById(customerId)
    CR->>DB: SELECT FROM Customers
    DB-->>CR: Customer record
    CR-->>OP: Customer object

    OP->>DB: SELECT Price, StockCount FROM Products
    DB-->>OP: unitPrice, stockCount

    Note over OP: Calculate subtotal + tax + loyalty discount

    OP->>PS: ChargeCustomer(email, total, token)
    PS->>GW: POST /v2/charge
    GW-->>PS: "APPROVED"
    PS-->>OP: true

    OP->>DB: BEGIN TRANSACTION
    OP->>DB: INSERT INTO Orders
    DB-->>OP: orderId
    OP->>DB: UPDATE Products (reduce stock)
    OP->>CR: AddLoyaltyPoints(customerId, points)
    CR->>DB: UPDATE Customers
    OP->>DB: COMMIT

    OP->>SMTP: Send confirmation email
    SMTP-->>OP: OK

    OP-->>Client: orderId
```

## Failure-Path Flow Chart

```mermaid
flowchart TD
    A[ProcessOrder called] --> B{Customer exists?}
    B -- No --> ERR1[Log error, return -1]
    B -- Yes --> C{Customer active?}
    C -- No --> ERR2[Log error, return -1]
    C -- Yes --> D{Product found?}
    D -- No --> ERR3[Log error, return -1]
    D -- Yes --> E{Stock sufficient?}
    E -- No --> ERR4[Log insufficient stock, return -1]
    E -- Yes --> F[Calculate total + tax + discount]
    F --> G{Payment approved?}
    G -- No --> ERR5[Log payment failure, return -1]
    G -- Yes --> H[Begin DB transaction]
    H --> I{Insert order + update stock + add points}
    I -- Exception --> J[Rollback transaction, return -1]
    I -- Success --> K[Commit transaction]
    K --> L[Send confirmation email]
    L -- Failure --> M[Log email error, continue]
    L -- Success --> N[Return orderId ✅]
    M --> N
```

## Data Flow Diagram

```mermaid
flowchart LR
    subgraph Input
        CID[customerId]
        PROD[productCode]
        QTY[quantity]
        TOK[paymentToken]
    end

    subgraph "OrderProcessor"
        VAL[Validate Customer]
        STOCK[Check Stock]
        CALC[Calculate Total]
        PAY[Process Payment]
        SAVE[Save Order]
        EMAIL[Send Email]
    end

    subgraph "External Systems"
        SQLDB[(SQL Server)]
        GATEWAY[QuickPay API]
        MAIL[SMTP Server]
    end

    CID --> VAL
    VAL --> SQLDB
    PROD --> STOCK
    QTY --> STOCK
    STOCK --> SQLDB
    CALC --> PAY
    TOK --> PAY
    PAY --> GATEWAY
    SAVE --> SQLDB
    EMAIL --> MAIL
```

---

*These diagrams were generated by Copilot CLI. Paste into any Mermaid-compatible renderer (GitHub markdown, VS Code preview, Mermaid Live Editor) to visualize.*
